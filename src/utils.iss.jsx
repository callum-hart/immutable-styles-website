import { createStyle } from 'immutable-styles';
// TODO: regex needs to handle ./ paths aswell as ../
// import { width } from './constants/breakpoints';

export default (
  <br maxWidth={/*width.toTablet*/767} className="hide-below-desktop">
    display: none;
  </br>
);
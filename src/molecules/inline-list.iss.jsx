import { createStyle } from 'immutable-styles';

export default (
  <ul className="inline-list">
    padding: 0;
    display: flex;

    <li>
      list-style: none;
      margin-right: var(--size-s);
    </li>
  </ul>
);

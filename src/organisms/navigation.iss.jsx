import { createStyle } from 'immutable-styles';

export default (
  <nav>
    <section className="container">
      display: flex;
      justify-content: space-between;
      align-items: center;
    </section>
  </nav>
);